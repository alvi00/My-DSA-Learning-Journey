#include<bits/stdc++.h>
using namespace std;

void learn_max_element(){
    int a[10];
    int max=*max_element(a,a+10);
    int min=*min_element(a,a+10);
}

int main() {
    learn_max_element();
    return 0;
}
