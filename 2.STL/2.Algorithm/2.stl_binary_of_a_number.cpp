#include<bits/stdc++.h>
using namespace std;

void learn_binary() {
    int num = 7;
    int binary = __builtin_popcount(num);

    //for long long values
    long long int num1=100000;
    int binary1=__builtin_popcount(num1);
}

int main() {
    learn_binary();
    return 0;
}
