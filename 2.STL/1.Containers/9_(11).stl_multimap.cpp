#include <bits/stdc++.h>
using namespace std;
// this is similar to map
// but here all the key values ar sorted
// but u can store same key values
// like key values are not unique
void learn_mulmap()
{
    multimap<int, double> alvi;
}
int main()
{
    learn_mulmap();
    return 0;
}
